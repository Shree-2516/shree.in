Frontend File Upload (Admin)
<input type="file" name="photo" />
<input type="file" name="resume" />
(We’ll connect this properly in next step)

When change update
git add .
git commit -m "Update project"
git push